<h3>以下の内容で、送信する。</h3>

<%= form_with(model: @feed, url: feeds_path, local: true) do |form| %>
  
  <%= form.hidden_field :content %>


  <p>本文:<%= @feed.content %></p>
  <%= form.submit  "登録する" %>
<% end %>
